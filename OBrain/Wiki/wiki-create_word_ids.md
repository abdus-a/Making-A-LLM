# wiki-create_word_ids · `Functions/create_word_ids.py`

`create_word_ids(vocabulary) -> dict[str,int]`. Assigns each vocab word a sequential integer id
starting at 0, in vocabulary order. This dict is the word↔number bridge the model will use.
